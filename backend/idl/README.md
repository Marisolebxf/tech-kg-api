# IDL

接口定义文件放在此目录。

当前接口主要由 FastAPI 路由和 Pydantic 模型直接定义；当需要沉淀跨服务或前后端共享的接口契约时，再在本目录补充 IDL 文件。

建议放置内容：

| 类型 | 文件建议 | 说明 |
|---|---|---|
| HTTP 接口契约 | `*.openapi.yaml` | 固化对外 API 的路径、请求、响应和错误码 |
| 事件/消息契约 | `*.jsonschema.json` | Kafka 等消息结构定义 |
| 跨语言 RPC 契约 | `*.proto` | 需要生成多语言客户端时使用 |
