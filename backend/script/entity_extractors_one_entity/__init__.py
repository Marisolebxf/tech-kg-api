"""One-entity-per-script extraction entrypoints.

These scripts are compatibility wrappers around the existing production ETL
code. They live in a separate folder so the current scripts remain untouched.
"""
